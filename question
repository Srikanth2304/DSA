/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* removeNthFromEnd(ListNode* head, int n) {
        ListNode* temp = head;
        int count = 0;
        while(temp != nullptr){
            count++;
            temp = temp->next;
        }
        count = count - n;
        cout<< count;
        ListNode * temp2 = head;
        for(int i = 0 ;i < count ; i++){
            temp2 = temp2->next;
        }
        ListNode * temp1 = head;
        while(temp1->next != nullptr and temp1->next != temp2){
            temp1 = temp1->next;
        }
        temp1->next = temp1->next->next;
        return head;
    }
};




Line 30: Char 36: runtime error: member access within null pointer of type 'ListNode' (solution.cpp)
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior prog_joined.cpp:39:36
